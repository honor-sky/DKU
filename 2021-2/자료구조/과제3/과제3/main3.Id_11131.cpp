#include <stdio.h>
#include <string.h>
#include "stack_PM.h"

//int main(void) {
//
//	char str[] = "(((a+b)*c+d-e)/(f+g)-(h+j)*(k-l))/(m-n)";
//	char str2[] = "(a−b*c)+d−(e−(f−g)/h)/(i*j)−k";
//
//	stack(str, strlen(str));
//	stack(str2, strlen(str2));
//
//
//	return 0;
//}